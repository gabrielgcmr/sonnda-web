// src/app/router/routes.ts
export const routes = {
  root: '/',
  login: '/login',
  register: '/register',
  confirmEmail: '/confirm-email',
  onboarding: '/onboarding',
  patients: '/app',
} as const
